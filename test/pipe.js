var assert = require('assert');
var Pipe = require('../src/dependencies/pipe').Pipe


describe('createPipe()', function() {
    it('should create a pipe with a given ID', function() {
      
  });
});